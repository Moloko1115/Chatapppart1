# Chatapppart1

Login logic application written in Java.

## Project structure

The Maven project is located in `chartapppart/` and contains the login implementation, application entry point, and JUnit tests.

## Build and test

From the project directory, run:

```bash
mvn test
```

To run the application:

```bash
mvn exec:java
```
