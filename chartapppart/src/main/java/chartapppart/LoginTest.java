package chartapppart;

/**
 * Legacy placeholder retained for compatibility with the original project layout.
 * Tests belong in src/test/java, so this class intentionally contains no JUnit code.
 */
public final class LoginTest {
    private LoginTest() {
        // Utility class; do not instantiate.
    }
}
